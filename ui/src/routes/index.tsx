import { Title } from "solid-start";
import Counter from "~/components/Counter";

export default function Home() {
  return (
    <main>
      <button>Start a New Session</button>
      <button>Join an Existing Session</button>
    </main>
  );
}
